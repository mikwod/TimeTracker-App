<UserControl x:Class="TimeTracker.Task"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeTracker"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="300">
    <Grid Margin="0 0 0 0">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="TaskText" TextAlignment="Center" Text="Task 1" Grid.Column="0"></TextBlock>
        <Button x:Name="StartStopButton" Click="StartStopButton_Click" Content="Button" Grid.Column="1"></Button>
        <TextBlock TextAlignment="Center" x:Name="TimeText" Text="No Time" Grid.Column="2"></TextBlock>
    </Grid>
</UserControl>
